const ping = require('./ping');
const login = require('./login');
const getUser = require('./users');
const topUser = require('./topUsers');

module.exports = {
  ping,
  login,
  getUser,
  topUser,
};
