const firstFunction = (string) => string.toUpperCase();

const secondFunction = (string) => string.chartAt(0);

const thirdFunction = (string1, string2) => string1.concat(string2);

module.exports = {
  firstFunction,
  secondFunction,
  thirdFunction
};
